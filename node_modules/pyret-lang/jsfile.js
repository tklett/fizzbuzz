function f() {
  return 42;
}

({
provides: { values: { f: ["arrow", [], "Number"] } },
requires: [],
nativeRequires: [],
theModule: function(runtime, _, uri) {
  return runtime.makeModuleReturn({
    f: runtime.makeFunction(f)
  }, {});
}

})
